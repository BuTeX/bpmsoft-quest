# Статус проекта BPMSoft Quest

Последнее обновление: 2026-07-20.

Это каноническая точка входа в проект для новой сессии. Детали вынесены в:

- [`docs/SESSION_HANDOFF.md`](docs/SESSION_HANDOFF.md) — что прочитать и сделать в новой сессии;
- [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) — устройство приложения и правила расширения;
- [`docs/ROADMAP.md`](docs/ROADMAP.md) — завершённые и следующие этапы;
- [`docs/QA.md`](docs/QA.md) — проверки и критерии готовности;
- [`docs/SOURCES.md`](docs/SOURCES.md) — карта локальных источников BPMSoft;
- [`bpmsoft-quest-missions-mvp.md`](bpmsoft-quest-missions-mvp.md) — содержание миссий;
- [`mvp-learning-path.md`](mvp-learning-path.md) — продуктовая структура трека.

## 1. Активная рабочая копия

Продолжать работу нужно здесь:

```text
/Users/viktordulec/Yandex.Disk-v.dulec.localized/Codex/Projects/.integration-harbor-work
```

Каталог `/Users/viktordulec/Yandex.Disk-v.dulec.localized/Codex/Learning` использовался как источник прежней версии и ассетов. Он не содержит последних изменений из этой рабочей копии; не заменять им текущие файлы.

## 2. Продукт

«Академия Гуд программ | BPMSoft Quest» — игровая обучалка по функционалу и low-code возможностям BPMSoft. Это карта продукта, а не курс по общей профессии аналитика.

Игрок решает короткие практические ситуации, выбирает инструменты BPMSoft, получает XP, тратит low-code energy на ошибки и последовательно открывает зоны фэнтезийной карты.

Текущая версия — статическое веб-приложение на HTML, CSS и JavaScript без сборки и внешних runtime-зависимостей. Прогресс хранится в `localStorage` под ключом `bpmsoft-quest-v1`.

## 3. Текущий результат

Реализовано 9 из 9 миссий. Первый город завершён.

### Уровень I. Основы платформы — завершён

| № | Зона | Механика | Награда | Суммарный XP |
|---|---|---|---:|---:|
| 01 | Interface Hub | последовательность терминов интерфейса | до 55 | 55 |
| 02 | Data Forge | сопоставление терминов данных | до 45 | 100 |
| 03 | Access Citadel | последовательный quiz по доступам | 50 | 150 |
| 04 | Process Engine | сборка low-code процесса | 55 | 205 |
| 05 | Case Arena | настройка DCM-маршрута прямо на арене | 45 | 250 |

После миссии 05 открывается уровень II.

### Уровень II. Архитектура решения — завершён

| № | Зона | Состояние | Награда | Суммарный XP |
|---|---|---|---:|---:|
| 06 | Integration Harbor | пять интерактивных причалов и три контрольных сигнала | 100 | 350 |
| 07 | Insight Tower | последовательный диалог с проводником | 50 | 400 |
| 08 | Solution Gate | 12 требований и пять повторно используемых категорий | 50 | 450 |
| 09 | City Nexus | шесть узлов решения и три приёмочных испытания | 50 | 500 |

После миссии 09 интерфейс показывает `250 / 250 XP` текущего уровня и отдельный финальный экран первого города.

## 4. Что работает

- карта мира и журнал зон;
- последовательная разблокировка миссий 01–09;
- режимы `sequence`, `matching`, `quiz`, `gateway`, `classification`, `blueprint`;
- XP, шкала текущего уровня и три единицы low-code energy;
- штрафы, подсказки, повторные попытки и повторное прохождение без повторной награды;
- сохранение и канонизация прогресса через `localStorage`;
- админ-режим с парольным входом в рамках вкладки, подтверждением выхода и фиолетовой подсветкой правильного ответа для текущего вопроса или активного узла;
- перемешивание вариантов во всех миссиях при каждом новом запуске: правильный ответ не остаётся первым, а палитры не сохраняют канонический порядок решения;
- отдельные панорамы миссий, карта, портрет проводника, официальный логотип и локальный Unbounded;
- интерактивная карточка миссии 01: панорама, маршрут и пять слотов связаны в один живой сценарий;
- интерактивная миссия 02 «Кузница данных»: пять терминов назначаются непосредственно конструкциям на панораме; desktop поддерживает drag-and-drop и выбор кликом, mobile — камеру-фокус и навигацию по узлам 1–5;
- интерактивная миссия 03 «Стражи доступа»: два стража и три рубежа размещены на панораме, вопрос выводится под активной цифрой, а ответы назначаются печатями через drag-and-drop или клик;
- интерактивная миссия 04 «Энергетический конвейер»: пять модулей устанавливаются непосредственно в узлы Process Engine на панораме; активный узел раскрывает свой вопрос, правильные модули фиксируются после проверки, ошибочные выталкиваются обратно в палитру;
- интерактивная миссия 05 «Маршрут по арене»: пять элементов DCM-кейса назначаются физическим точкам Case Arena; после ошибки правильные узлы остаются закреплёнными, а неверные назначения возвращаются в палитру;
- интерактивная миссия 06 «Пять причалов сигнала»: webhook-шлюз собирается непосредственно на панораме Integration Harbor, после чего все причалы остаются запитанными во время трёх контрольных тестов;
- интерактивная миссия 07 «Совет проводника»: пять управленческих вопросов проходят как последовательный диалог с наставником поверх панорамы; неверная реплика получает мгновенную реакцию, а прогресс отмечается пятью печатями;
- интерактивная миссия 08 «Коробка или доработка»: 12 требований независимо назначаются пяти многоразовым категориям кликом или drag-and-drop; после ошибки верные карточки закрепляются, а первая ошибка получает объяснение и разбор ловушки;
- финальная миссия 09 «Собери мини-решение»: шесть независимых узлов City Nexus комплектуются уникальными модулями кликом или drag-and-drop; после сборки решение проходит приёмку регистрации, модели доступа и эскалации;
- отдельный финальный экран с итогами `9 / 9`, `500 XP`, `250 / 250`, достижением «Архитектор первого города» и анонсом следующей карты в другой стилистике;
- адаптивный интерфейс desktop/mobile;
- запуск локально и в LAN через `start-lan.sh`.

## 5. Последние проверки

- `node --check app.js` — успешно;
- `node --check test-mission.mjs` — успешно;
- `node test-mission.mjs` — успешно, полный маршрут 01–09;
- browser smoke-тест `1440 × 1000` и `390 × 844` — успешно;
- горизонтального переполнения и ошибок консоли не обнаружено;
- задания 01 и 07 пройдены интерактивно с корректным XP;
- обновлённая миссия 01 проверена на desktop и `390 × 844`: пять маркеров реагируют на выбор, горизонтального overflow нет;
- обновлённая миссия 02 проверена на desktop и `390 × 844`: пять узлов сцены интерактивны, правильные назначения фиксируются после ошибки, неверные возвращаются в палитру, mobile-фокус не перекрывает подсказку;
- обновлённая миссия 03 проверена на desktop и `390 × 844`: неверная печать подсвечивается и расходует energy, правильные рубежи фиксируются, финальное состояние содержит пять открытых точек и `150 XP`;
- обновлённая миссия 04 проверена на desktop и `390 × 844`: пять узлов конвейера связаны с деталями иллюстрации, после ошибки три правильных модуля фиксируются, два неверных выталкиваются, финальный запуск даёт `205 XP` и открывает Case Arena; горизонтального overflow и ошибок консоли нет;
- обновлённая миссия 05 проверена на desktop и `390 × 844`: пять точек маршрута расположены на арене, после перестановки первых двух элементов остальные три фиксируются, неверные назначения выталкиваются, успешный маршрут даёт `250 XP`;
- обновлённая миссия 06 проверена на desktop и `390 × 844`: пять причалов связаны с объектами панорамы, частично правильная конфигурация сохраняется после ошибки, успешная сборка переводит сцену в фазу трёх контрольных сигналов и даёт `350 XP`;
- обновлённая миссия 07 проверена на desktop и `390 × 844`: диалог с проводником содержит пять вопросов и четыре реплики на каждом шаге, неверный ответ не переводит диалог дальше, успешное завершение даёт `400 XP`;
- миссия 08 проверена на desktop `1440 × 1000` и mobile `390 × 844`: 12 карточек и пять корзин не обрезаются, категории повторно используются, неполная проверка блокируется, старое сохранение 01–07 открывает Solution Gate, завершение даёт `450 XP` и `200 / 250 XP` уровня;
- `assets/mission-solution-gate.png` проверен в размере `1600 × 500`, HTTP-ответ — `200`;
- миссия 09 проверена на desktop `1440 × 1000` и mobile `390 × 844`: шесть узлов и девять модулей не обрезаются, сборка переводит игру в три приёмочных испытания, завершение даёт `500 XP` и `250 / 250 XP`, replay не выдаёт XP повторно;
- старое сохранение 01–08 канонически открывает City Nexus с `450 XP`;
- финальный экран проверен на desktop/mobile: `scrollWidth = viewportWidth`, обе кнопки читаемы, консоль не содержит error/warning;
- `assets/mission-city-nexus.png` проверен в размере `1600 × 500`;
- шрифты вопросов во всех механиках увеличены: на интерактивных панорамах — с `8 px` до `11–13 px`, в matching-карточках — до `13–15 px`, в quiz-карточках — до `18–23 px`; длинные вопросы проверены на desktop и `390 × 844` без переполнения;
- `mission-insight-tower.png` проверен в размере `1600 × 500`.
- админ-режим проверен на desktop и `390 × 844`: неверный пароль показывает ошибку, верный включает подсветку `#7856ff`, отмена выхода сохраняет режим, подтверждение отключает его; горизонтального overflow нет.
- широкий логотип больше не используется как favicon: подключены отдельная квадратная монограмма `16 × 16`, `32 × 32`, мастер `512 × 512` и `apple-touch-icon` `180 × 180`.

Подробности находятся в [`docs/QA.md`](docs/QA.md).

## 6. Запуск после новой загрузки компьютера

Из активной рабочей папки:

```bash
./start-lan.sh
```

Локальный адрес по умолчанию:

```text
http://127.0.0.1:4173/
```

LAN-адреса выводятся самим скриптом и могут измениться после перезагрузки или переподключения к сети. Сервер предыдущей сессии после выключения компьютера не сохраняется.

## 7. Следующая задача

Первый город считать закрытой главой. Следующим игровым этапом должна стать отдельная карта в другой стилистике с усложнёнными заданиями.

До разработки второй карты нужно отдельно зафиксировать её тему, визуальный язык, состав миссий, новые механики, прогрессию и правила перехода между картами. Не переделывать текущий город под новую стилистику и не смешивать его завершённую прогрессию `0–500 XP` с будущей главой.

Порядок подготовки записан в [`docs/ROADMAP.md`](docs/ROADMAP.md) и [`docs/SESSION_HANDOFF.md`](docs/SESSION_HANDOFF.md).

## 8. Известные ограничения

- активная рабочая папка ещё не является отдельным Git-репозиторием;
- пароль админ-режима хранится в клиентском JavaScript и подходит только для учебной подсветки, а не для защиты данных;
- вторая карта, её стилистика и усложнённые задания пока не спроектированы;
- `image-prompts.md` в корне и `assets/image-prompts.md` дублируют часть информации; каноническим считать файл в `assets/`;
- `mission-integration-harbor.png` в корне — лишняя копия; приложение использует `assets/mission-integration-harbor.png`.
